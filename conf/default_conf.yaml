redis:
  addr:     "localhost:6379"
  password:  ''  # no password set
  db:       0   #use default DB
mysql:
  username: "root"
  password: "123456"
  addr: "127.0.0.1:3306"
  database: "douyin_app"
jwt:
  token_expire_duration: 240    #hour
  secrect: "天王盖地虎"           #token服务端秘钥
server:
  port: "8080"

#配置信息格式模板,使用前先把 default_conf.yaml 改为 conf.yaml